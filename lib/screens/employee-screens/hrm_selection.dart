import 'package:flutter/material.dart';

class HrmSelection extends StatelessWidget {
  const HrmSelection({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(child: Text("HrmSelection"));
  }
}
